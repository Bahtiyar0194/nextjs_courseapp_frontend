const Loader = (props) => {
        return (
            <div className={props.className}>
                <div className="overlay-loding-circle"></div>
            </div>
        );
};

export default Loader;